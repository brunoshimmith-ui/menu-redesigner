import { PageShell } from "@/components/PageShell";

const Transferencias = () => (
  <PageShell
    title="Transferências"
    description="Realize e acompanhe solicitações de transferência de alunos."
  />
);

export default Transferencias;
